using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KingKong : MonoBehaviour
{

    //Drum prefabs
    [SerializeField]
    private GameObject drumPrefabs;

    //Yelling

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //Throw Drums - called from animation
    void ThrowDrums()
    {
        //Initialise the drum prefabs   
    }
}
